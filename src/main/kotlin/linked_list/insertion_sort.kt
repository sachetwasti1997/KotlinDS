package linked_list

class Solution18 {
    fun insertionSortList(head: ListNode?): ListNode? {
        var t = head?.`val`?.let{ListNode(it)}
        head?.next?.let{

        }
        return head
    }
}